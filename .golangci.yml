run:
  concurrency: 6
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - goimports
    - revive
    - bodyclose
    - ineffassign
    - tparallel
    - unconvert
    - wastedassign
    - whitespace
    - go-critic
    - gocyclo

linters-settings:
  go-critic:
    enable-all: true
  gocyclo:
    min-complexity: 15

version: "2"